[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-builder-mcp-client-aws-transform"
version = "1.0.1"
description = "Python MCP client provided by the AWS Transform team"
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "AWS Transform Team" }]
license = "Apache-2.0"
license-files = ["LICEN[CS]E*", "NOTICE*", "THIRD-PARTY-NOTICES.txt"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Operating System :: OS Independent",
]
dependencies = [
    # Pin to the mcp 1.x major the toolkit is tested against; mcp 2.0.0 is a
    # breaking release (see issue #77). Widen once verified against 2.x.
    "mcp>=1.0,<3.0",
    "anyio>=4.0",
    "trio>=0.20",
]

[project.optional-dependencies]
test = ["pytest"]

[tool.setuptools.packages.find]
where = ["src"]
